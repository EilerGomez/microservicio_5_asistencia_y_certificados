# application-dev.yml
server:
  port: 8085

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/db_asistencia_certificados?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: mysql
    driver-class-name: com.mysql.cj.jdbc.Driver